import XCTest
@testable import coursesU_iOS_template

final class coursesU_iOS_templateTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(coursesU_iOS_template().text, "Hello, World!")
    }
}
