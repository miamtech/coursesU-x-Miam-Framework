import XCTest
@testable import CoursesUIOSTemplate

final class coursesU_iOS_templatesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CoursesUIOSTemplate().text, "Hello, World!")
    }
}
