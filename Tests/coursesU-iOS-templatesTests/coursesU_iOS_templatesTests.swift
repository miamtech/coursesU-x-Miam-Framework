import XCTest
@testable import coursesU_iOS_templates

final class coursesU_iOS_templatesTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(coursesU_iOS_templates().text, "Hello, World!")
    }
}
